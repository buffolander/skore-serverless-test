{
  "name": "skore-serverless-test",
  "version": "0.0.1",
  "author": "Bruno Soares",
  "scripts": {
    "test": "jest",
    "watch": "jest --watch */*.js"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "aws-sdk": "^2.399.0",
    "papaparse": "^4.6.3"
  },
  "devDependencies": {
    "jest": "^24.1.0"
  }
}
